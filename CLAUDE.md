# CLAUDE.md / AGENTS.md — AIエージェント運用則（汎用テンプレ）

著作権: RTCK

実際の数ヶ月の開発ログ（Claude系 / Codex(GPT)系の両方）から抽出した、再発した失敗の対策だけを残したリポジトリ層の運用則。
ユーザー個人設定（口調・思考スタイル等）とは別レイヤ。ここにはエンジニアリング上の「やらかし対策」だけを置く。
新規リポジトリにコピーして使う。Codex併用時は本ファイルを `AGENTS.md` にミラーする。

---

## 0. このファイルの使い方

- 各プロジェクト固有の事実（アーキテクチャ・確定値・ドメイン制約）は別途追記する。本ファイルは**プロジェクト非依存の運用則**のみ。
- 作業着手前に「§7 Pre-Edit チェックリスト」を通す。
- ルールに反する近道を見つけても取らない。理由は全部、過去に実際に事故ったから。

---

## 1. 完遂の権限は先に渡す ── 勝手に止まるな

- [ ] 複数工程を頼まれたら、**全工程を最後までやりきる**。途中で選択肢を並べて確認待ちにしない。
- [ ] 「どれからやりますか？」と聞く代わりに、全部やってから「全部やった上で、特に〜」と報告する。
- [ ] 作業中のユーザー補足を**「却下」と誤読して止まらない**。割り込み ≠ 中止指示。
- [ ] 例外（事前確認が必要）は、後戻りコスト大の操作のみ：ファイル削除・外部API課金・既存データ上書き・破壊的スキーマ変更。

> なぜ: 賢いモデルほど気を回して途中で止まりたがる。最も多かったユーザーの発言が「なんで止まったの？」だった。

---

## 2. 「動いた」と「正しい」は別もの ── 完了の定義を握る

- [ ] **silent failure 禁止**。依存・機能が無いときは空の結果で成功扱いにせず、**明示的に失敗（exit ≠ 0 + 原因とインストールヒント）**する。
- [ ] **smoke test pass ≠ production parity pass**。小データで通っても、**本番サイズ・本番パターンのデータで必ず再検証**する。
- [ ] 「テストが緑」を完了としない。**観測可能な成功条件**（本物の入力で期待出力が出る）を満たして初めて完了。
- [ ] UIだけ・CRUDのCだけ、のような**部分実装で「できました」と言わない**。完結する単位で動かす。
- [ ] テストを期待値に書き換える/skip/コメントアウトで「通過」を演出しない。落ちる原因を直す。

> なぜ: 緑のチェックとドキュメントの「✅完了」が一番危ない。ライブラリの量依存サイレント破壊、機能なしで成功を返す実装は、後から発見するほど高コスト。

---

## 3. 数字が良くても、目が「ダメ」なら負け ── 最終裁定は人間の目

- [ ] **代理指標（測りやすい数字）を最適化していないか**を常に疑う。その指標は本当に欲しい品質を測っているか。
- [ ] 「指標↑ ＋ 見た目↓」が同時に起きたら、**副作用と本来の目的が混ざっているサイン**として扱う。
- [ ] 視覚・体感が関わる成果物は、**人間の目視評価を最終 ground truth** にする。数値は補助線。必要なら複数候補を並列で見せる。

> なぜ: 「67%改善」報告の裏で出力が壊れていた。最適化していた的がそもそもズレていた。

---

## 4. 真実の出所はひとつ ── 二重定義は静かに全部を毒する

- [ ] 定数・設定・幾何値などの**source of truth は1ファイルに固定**。他ファイルへの複製禁止。
- [ ] 同じ責務の操作を**2経路に持たない**。同じ操作は単一の出所からのみ呼ぶ（テストコードも含めて）。
- [ ] やむを得ず重複が生じる場合は冪等性をテストで担保する。

> なぜ: 片方だけ古い複製値が下流を静かに汚染し、何週間も気づかなかった。エラーが出ないから怖い。

---

## 5. 30分詰まったら、視点を変える ── 一人で悩まない

- [ ] 同じエラーで**30分以上 stuck したら、別モデル/別視点にクロスチェック**を投げる。
- [ ] 聞き方は「これで合ってる？(Yes/No)」ではなく**「他の解釈・別の根本原因は？」**。同じ前提で聞くと同じ盲点を共有するだけ。
- [ ] **自己評価が入力に従属していないか**疑う。評価の基準が検証対象から派生していたら、何を入れても「正解」が返る。評価指標は対象から独立に取る。
- [ ] リトライは対症療法。根本原因の特定（なぜ失敗していたか）を理解してから直す。偶然動いたら、その旨を報告する。

> なぜ: 「現状ほぼ正解です」が、自分で作った地図で自分の位置を確認していただけ、という事故があった。別AIに「別の解釈は？」と聞いて初めて気づいた。

---

## 6. 道具と設計の選び方 ── 凝る前に測る

- [ ] **コスト非対称が大きい技術**（業界標準ツールを自前再現しようとする系）は、早めに外部ツール導入を検討する。自前で数週間 < 専用ツール30分、はよくある。
- [ ] **「最終形」を盲信しない**。古い資料の「決定版」も再検証する。ライブラリ進化で前提が変わっている。
- [ ] 新機構を入れる前に、**シンプル版とベンチで比較**。複雑さに見合う改善があるか毎回測る。深い多段処理より単純版が良いことが多い（過剰設計はたいてい悪手）。
- [ ] 手法選択の前に**想定 use case を分類**する。ある文脈で正しい手法が、別の文脈では破綻する（必要精度を先に見積もる）。

> なぜ: 自前ロジック4経路で壁にぶつかり、避けていた外部ツールを入れたら30分で越えた。「最終形」も複雑化も、実測で何度も覆った。

---

## 7. Pre-Edit チェックリスト

着手前に確認：

- [ ] このファイル/値の source of truth はどこか。複製を増やそうとしていないか。（§4）
- [ ] これは実験か、本線の変更か。改善を証明する指標は何か。その指標は正しい的を指すか。（§3）
- [ ] 本番サイズ・本番パターンで検証する手順があるか。（§2）
- [ ] 依存が無いとき、明示 fail するか（silent skip していないか）。（§2）
- [ ] 後戻りコストの高い操作（削除・課金・上書き・スキーマ確定）か → だけは事前確認。（§1）
- [ ] 30分詰まったらクロスチェックに切り替える準備があるか。（§5）

長時間ジョブを始める前に：

- [ ] 重複ワーカーが走っていないか確認（プロセス散らかし防止）。
- [ ] むやみな sleep でなく PID/状態ベースで監視。
- [ ] 失敗時の記録（sentinel）と排他（lock）。

---

## 8. モデル別メモ（併用時の使い分け）

実戦で見えた傾向。固定の優劣ではなく、振りどころの目安。

- **Claude系が振りやすい**: 仮説駆動の切り分け、独立検算、前提への反論役、設計議論、後で戻せる粒度の変更。
- **Codex(GPT)系が振りやすい**: 長時間・重い処理の段取り（並列化・PID監視・リソース配分）、形式/バイナリ周りの泥臭いバグ取り、経験則→実装への落とし込み。
- **両者に共通の落とし穴**（＝エージェント全般の構造的癖）: 早すぎる完了宣言、プロセスの散らかし、source of truth の軽視、自分の指標を人間の目より信じる。→ §1〜§5 が効く。
- **採否の最終判断は人間が握る**。どちらにも渡さない。

---

## 9. プロジェクト固有（ここから下に追記する）

<!-- この線から下にプロジェクト固有の事実を書く。 上の §0〜§8 は触らない。
例:
- アーキテクチャ: …
- 確定値 / source of truth ファイル: …
- ドメイン制約: …
- ビルド / テストコマンド: …
- 完了の定義（このプロジェクト固有）: …
-->

### アーキテクチャ

macOS 向け SwiftUI ネイティブアプリ。VPN Gate（筑波大学の学術実験プロジェクト）が公開している
公開VPN中継サーバーの一覧を取得し、選んで OpenVPN で接続する。

```
Sources/
  Models.swift        VPNServer / VPNPolicy（候補数・一覧の鮮度しきい値の source of truth）
  VPNGateAPI.swift    CSV取得とパース
  VPNController.swift openvpn の起動・停止・監視、管理IF、空きポート確保、設定の無害化と検証
  Scripts.swift       root で走る5本のシェル（run / stop / up / down / selftest）のテンプレート
  AppModel.swift      状態管理・候補選択・一覧の鮮度管理・失敗の記憶
  ContentView.swift   UI
  TsukubaVPNApp.swift エントリポイント
Test/main.swift       静的検証（verify.sh）
Test/Live/main.swift  実サーバーを使う統合検証（livetest.sh）
```

実行時の作業フォルダは `~/Library/TsukubaVPN`（**`Application Support` ではない**。
openvpn の `--up` / `--down` はスペース入りパスを引数分割してしまうため）。

### 確定値 / source of truth

- `VPNPolicy.candidateCount = 5` — 候補数。ここ以外に数字を書かない
- `VPNPolicy.listStaleAfter = 600` — 一覧をこの秒数放置したら接続前に取り直す
- root で走るシェルは **すべて `Scripts.swift` の文字列が唯一の出所**。
  作業フォルダの `.sh` は毎回そこから書き出される生成物なので、直接編集しても次の接続で消える
- API: `https://www.vpngate.net/api/iphone/`

### ドメイン制約（それぞれ実際に踏んだ）

- **CRLF**: Swift では `\r\n` が1つの Character。素朴な split は一切マッチしない。
  行分割は必ず `String.normalizedLines` を通す
- **管理ポートを固定にしない**: 直前の接続の TIME_WAIT が残っていると
  `Socket bind failed (errno=48)` で全候補が即死する。接続のたびに
  SO_REUSEADDR なしで bind を試して空きを実測する
- **`--up` の非ゼロ終了は接続を殺す**: openvpn は `run_up_down` を `S_FATAL` で呼ぶ。
  DNS 切替の失敗で接続ごと落とさないため、`up.sh` / `down.sh` は必ず 0 で終わり、
  結果を `dns-status.txt` に残してアプリが警告する
- **接続処理の排他**: `run.lock` がないと、取り残された run.sh が
  次の openvpn を pid ファイル経由で殺しにいく
- **公開サーバーは頻繁に落ちている**: 上位3台が同時に死んで4台目で成功は普通に起きる。
  「1台目で失敗」はバグではない。短時間に何十回も接続すると `auth-failure` で断られる
- **設定は許可リスト方式で再構成**する。API 由来の文字列をそのまま openvpn に渡さない

### ビルド / テスト

```sh
./build.sh         # build/つくばVPN.app（ad-hoc署名まで）
./verify.sh [国]   # 静的検証
./livetest.sh [国] # 実サーバーを使う統合検証（65項目）
./record.sh start|stop|status   # 実接続時のブラックボックス記録
```

`livetest.sh` は `dev null` で「utun を作る工程だけ」を回避し、本番と同一のシェルを
実サーバーに対して走らせる。**ルート権限なしで到達できる限界まで通す**のが設計意図。

### 完了の定義（このプロジェクト固有）

1. `./build.sh` がエラーなく通る
2. `./verify.sh` が EXIT 0
3. `./livetest.sh` が失敗0件
4. **`/Applications/つくばVPN.app` に反映されている**
   （build/ に置いただけで「完成」と言わない。一度これで事故った）
5. 変更が接続経路に触るなら、`record.sh` で実接続を1回記録して `report.md` を確認する

### 実接続で確認済み（2026-08-08 15:55、219.100.37.115）

root での utun 作成 / `do shell script` 経由 openvpn の生存 / up.sh の実行 /
DNS の実書き換えと復元 / `redirect-gateway` による全経路切替 /
`remote-cert-tls server` の実効（VERIFY EKU OK）/ 管理IF経由の権限なし切断 /
経路とDNSの完全復元。ログは `Sources/Scripts.swift` の設計どおりに流れた。
